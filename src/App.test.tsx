import { App } from './App';
// https://testing-library.com/docs/example-react-router
describe('<App />', () => {
  it.skip('<App />', () => {
    // @ts-ignore
    const _ = App; // eslint-disable-line @typescript-eslint/no-unused-vars
  });
});
